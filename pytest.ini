[pytest]
# Root test discovery path
testpaths = tests

# Show short test IDs and verbose output by default
addopts = -v --tb=short

# Ensure the project root is on sys.path so `from app.xxx import ...` works
# without needing to install the package in editable mode.
pythonpath = .
